"""LLM Fine-Tuning project — reusable library code.

Subpackages:
    data      : Build, format, and deduplicate datasets.
    eval      : Run the evaluation suite and compute metrics.
    training  : LoRA fine-tuning entry points.
    inference : Load a fine-tuned model and generate answers.
"""
